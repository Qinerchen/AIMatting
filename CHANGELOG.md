# 更新日志

## [0.0.5] - 2026-08-11

### 修复

- 修复「模型管理」中点击下载必现的 TypeError 崩溃（参数传错导致应用内下载不可用）。
- 修复预处理调整在撤销后再次调整会双重叠加的问题；预处理调整（含抠图前）现在都可撤销。
- 修复任务进行中关闭窗口长时间无响应：取消/关闭会立即终止推理子进程，不再等待当前推理跑完。
- 修复下载中关闭「模型管理」对话框导致线程随对话框销毁的问题，并清理取消下载留下的 `.part` 残留。
- 修复保存时若后缀为空可能静默覆盖源文件的问题（自动追加安全后缀）。
- 批处理输出同名文件自动加 `(1)` 编号，避免互相覆盖；删除/清空列表后状态不再错位。
- 补上 README/教程中宣传的 Ctrl+R（开始抠图）、Ctrl+S（保存/导出）快捷键。

### 优化

- 历史快照 PNG 编码移到后台线程，4K 大图操作不再卡 UI。
- 去色边改为后台线程执行，可取消；画笔遮罩覆盖层按低分辨率生成，大图绘制更流畅。
- 吸管取色每帧只做一次全屏截图转换，高分辨率下帧率提升。
- 引擎会话启用启发式卷积搜索，避免首次推理长时间自动调优；新增 TensorRT 加速选项（失败自动回退）。
- 「推理分辨率」选项对固定输入模型明确标注暂不生效，并更新 README 性能说明。
- 启动入口（run.py）现在也会写入 crash.log，便于排查运行时异常。
- 移除不再使用的 `theme.py` / `good_window.py`；统一 `release_model_after_matte` 默认值。

## [0.0.4] - 2026-08-11

### 新增

- 背景颜色选择新增「吸管」屏幕取色功能：吸管跟随鼠标实时移动，
  覆盖全屏幕任意位置取色，按钮改为图标样式。
- 批量列表支持「批量设置」：选中多张图片时，「单独设置」按钮自动变为
  「批量设置」，对选中的所有图片应用同一套参数。
- 裁剪改为「双击」或 Enter 确认完成，单击图片外空白处不再误触裁剪。
- 裁剪后可撤销：Ctrl+Z 与左上角撤销按钮均可恢复裁剪前的完整状态。
- UI 细节优化：背景颜色选择器改为圆角色块；左右面板与中间画布的间隔
  统一为 1px 细灰分割线，整体更接近 Codex 的简洁风格。
- 性能优化：调整背景颜色 / 不透明度 / 预处理时，拖动过程使用低分辨率
  实时预览并防抖，松手后再全分辨率精算，大幅减少大图卡顿；
  预处理拖拽期间不再频繁写入历史记录。
- 裁剪框边框加粗、控制点放大，鼠标悬停时控制点高亮并切换拉伸光标，
  拖拽反馈更明显。
- 导入新图片时，预处理（亮度/对比度/饱和度/色温）自动归零，不再沿用
  上一张图的设置。
- 修复重做（Ctrl+Y）恢复到错误状态的问题，撤销/重做现在都正确。
- 批量页面增加底部状态栏，批量处理时也能看到状态与模型信息。
- 「适合窗口」后缩放百分比显示真实比例，不再固定显示 100%。
- 图片显示区域增加圆角与细边框，与整体风格统一。

### 修复

- 修复点击「单独设置 / 批量设置」后打开颜色选择窗口显示不全、需要手动
  拉大的问题。

### 其他

- 版本号更新为 0.0.4。

## [0.0.3] - 2026-08-11

- 界面升级为 PyQt-Fluent-Widgets（Fluent Design）现代化风格。
- 移除「手动微调 / 输出设置」页签的关闭叉号。
- 优化「抠图完成后自动释放模型内存」提示文字，避免显示不全。
- 版本号更新为 0.0.3。

## [0.1.0] - 2026-08-10

- 初始版本：BiRefNet 高精度抠图、笔刷遮罩、裁剪、批量抠图、
  GPU / CPU 自适应推理。
